let HID = require('../');
let REPL = require('repl');

let repl = REPL.start('node-hid> ');
let hid = new HID.HID(1356, 616);

console.log('features', hid.getFeatureReport(0xf2, 17));

hid.on('data', (data) => {
  console.log('got ps3 data', data);
});

repl.context.hid = hid;
